<%= record_form_for(@team) do |f| %>
  <% if @team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
      <% @team.errors.each do |attribute, msg| %>
        <%= content_tag(:li, error_for(attribute)) do %>
          <%= msg%>
        <% end %>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" >
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field"%>
    <%= f.label :competition %><br>
    <%= f.text_field :competition%>
  </div>
  <div class="field">
    <%= f.label :points %><br>
    <%= f.number_field :points%>
  </div>
  <div class="field">
    <%= f.label :ranking %><br>
    <%= f.number_field :ranking%>
  </div>
  <div class="actions">
    <%= f.submit "Save"%>
  </div>
<% end %>

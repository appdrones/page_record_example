<h1>Listing teams</h1>

<table>
  <thead>
    <tr>
      <th>Ranking</th>
      <th>Name</th>
      <th>Competition</th>
      <th>Games</th>
      <th>Points</th>
      <th>Ranking</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr <%= record_for(team)%> <%= class_name(team) %>>
        <%= attribute_tag_for(:td, :ranking) { team.ranking.to_s} %>
        <%= attribute_tag_for(:td, :name) { team.name} %>
        <%= attribute_tag_for(:td, :competition) { team.competition} %>
        <%= attribute_tag_for(:td, :number_of_games) { team.number_of_games.to_s} %>
        <%= attribute_tag_for(:td, :points) { team.points.to_s} %>
        <%= attribute_tag_for(:td, :ranking) { team.ranking} %>
        <td><%= link_to 'Show', team, action_for(:show) %></td>
        <td><%= link_to 'Edit', edit_team_path(team), action_for(:edit) %></td>
        <td><%= link_to 'Destroy', team, {method: :delete, data: { confirm: 'Are you sure?' }}.merge(action_for(:destroy)) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team', new_team_path, action_for(:new) %>
